#THIS WILL NOT FACTOR IN THE JUICE --- DECIMAL ODDS != IMPLIED ODDS
#Actual break even percentage(bep) will be slightly higher

odds = input("Enter American sports betting odds ")
fodds = float(odds)
if fodds < 0:
  fodds = fodds * -1
  bep = (fodds / (100 + fodds)) * 100
else:
  fodds > 0
  bep = 1 + (fodds/100)
print(f"{bep} %" + ' decimal odds')
